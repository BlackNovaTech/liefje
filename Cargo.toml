[package]
authors = ["Jur van den Berg <jur@vdberg.io>"]
edition = "2021"
name = "liefje"
version = "0.1.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
lief = "0.17"
napi = "3.0.0"
napi-derive = "3.0.0"

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
strip = "symbols"
